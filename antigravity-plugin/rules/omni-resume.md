# Omni Resume Rules

- Prefer the unified index for discovery.
- Keep automatic ingestion outside the IDE plugin.
- Do not duplicate transcript content into memory systems unless the workspace explicitly requires it.
- Use source-specific adapters for Codex, Hermes, Antigravity, Claude, and OpenCode session formats.

